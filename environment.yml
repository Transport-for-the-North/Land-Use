name: normits_lu
channels:
  - conda-forge
dependencies:
  - openpyxl=3.1.2
  - pip=24.0
  - python=3.11.8
  - pandas>2
  - numpy<2
  - pyyaml=6.0.1
  - seaborn=0.13.2
  - matplotlib=3.9.1
  # Setup for interactive maps
  - folium=0.17.0
  - mapclassify=2.5.0
  # Only used for memory usage tracking
  - psutil=6.1.0
  - pip:
      - git+https://github.com/Transport-for-the-North/caf.base
      - git+https://github.com/Transport-for-the-North/caf.space
      - git+https://github.com/Transport-for-the-North/caf.brain@machine_vision
